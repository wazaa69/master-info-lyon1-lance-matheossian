#ifndef _TEMPORAIRE_
#define _TEMPORAIRE_

#include "Symbole.hpp"
/**
 * Gestion d'une constante.
 */
class Temporaire  {
       
	public:

	/**
        *   @brief Constructeur, initialise le Temporaire
	*   @param _id identifiant du symbole
        */
        Temporaire(int _id);

        /**
        *   @brief Destructeur
        */
        ~Temporaire();	
	
};


#endif

